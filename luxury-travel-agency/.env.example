# Backend API URL (optional)
# If not set, the frontend will use /api (same server)
# For separate backend, set this to your backend URL (without /api at the end)
# Example: VITE_API_URL=https://your-backend-url.com/api
VITE_API_URL=
